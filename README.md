V1.5版本，重新使用了Webview编写，占用从150M（PyQt版本）下降到60MB，同时启动时间大幅度下降，并且添加了任务栏托盘，不需要时可右键退出，同时优化了动画逻辑，触发动画的范围大幅度缩小，防止误触，在展开前固定显示时间
此程序有较多的逻辑行为由AI进行修改（人话：技术太菜了，写不出来了）如遇BUG，请及时报告

(当运行此程序时如果窗口变白，不透明，不显示等若干问题，请退出后重新启动，一般两到三次就会正常）


Python 3.13
